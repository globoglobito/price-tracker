apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-secret
  namespace: price-tracker
type: Opaque
data:
  # Default credentials (base64 encoded)
  # Username: admin, Password: admin123
  # To change: echo -n "your-username" | base64
  username: YWRtaW4=
  # To change: echo -n "your-password" | base64  
  password: YWRtaW4xMjM=
